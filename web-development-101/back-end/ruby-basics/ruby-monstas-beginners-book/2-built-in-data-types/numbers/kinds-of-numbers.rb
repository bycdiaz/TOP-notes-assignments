# When doing calculations, integers and decimals will
# behave slightly differently.

1 + 2
# will always return integers

1.0 + 2
# will always return decimal

puts 3 / 2

puts 3.0 / 2

# when doing division, use floating point (decimals) for more accurate answer